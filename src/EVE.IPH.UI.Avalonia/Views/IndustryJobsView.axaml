<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="EVE.IPH.UI.Avalonia.Views.IndustryJobsView">
  <Grid Margin="20" RowDefinitions="Auto,*" RowSpacing="12">
    <StackPanel Spacing="4">
      <Button HorizontalAlignment="Left"
              Content="Refresh Industry Jobs"
              Click="RefreshIndustryJobs_Click"
              IsEnabled="{Binding CanRefresh}" />
      <TextBlock Opacity="0.8" Text="{Binding SummaryText}" TextWrapping="Wrap" />
    </StackPanel>

    <ListBox Grid.Row="1" ItemsSource="{Binding Items}">
      <ListBox.ItemTemplate>
        <DataTemplate>
          <Border Margin="0,0,0,8"
                  Padding="12"
                  CornerRadius="10"
                  Background="#16263B"
                  BorderBrush="#28415F"
                  BorderThickness="1">
            <Grid ColumnDefinitions="3*,2*,Auto" ColumnSpacing="16">
              <StackPanel>
                <TextBlock FontWeight="SemiBold" Text="{Binding BlueprintName}" />
                <TextBlock Opacity="0.75" Text="{Binding ActivityName}" />
              </StackPanel>

              <StackPanel Grid.Column="1">
                <TextBlock Text="{Binding InstallerName}" />
                <TextBlock Opacity="0.75" Text="{Binding ScopeText}" />
              </StackPanel>

              <StackPanel Grid.Column="2" HorizontalAlignment="Right">
                <TextBlock HorizontalAlignment="Right" Text="{Binding StateText}" />
                <TextBlock HorizontalAlignment="Right" Opacity="0.75" Text="{Binding StatusText}" />
              </StackPanel>
            </Grid>
          </Border>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>
  </Grid>
</UserControl>